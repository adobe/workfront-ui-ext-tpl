        mainMenu: {
          getItems() {
            return [
              <%_ extensionManifest.mainMenuItems.forEach((button, index) => { -%>
              {
                id: '<%- button.id %>',
                url: '/index.html#/<%- button.id %>',
                label: '<%- button.label %>',
                icon: <%= index % 2 === 0 ? 'icon1' : 'icon2' %>,
              },
              <%_ }) -%>
            // @todo YOUR HEADER BUTTONS DECLARATION SHOULD BE HERE
            ];
          },
        },
